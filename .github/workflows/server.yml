name: "server"

on:
  workflow_call:
  workflow_dispatch:

jobs:
  build:
    runs-on: Windows
    steps:
    # This step checks out a copy of your repository.
    - name: Checkout repository
      uses: actions/checkout@v2
    
    - name: Compile
      run: ls
